## TDD, strictly
Write ONE failing test, run it and see it red, then write the minimal code to make it green, then refactor. Never write source ahead of a failing test and never batch several tests. Tests are the oracle: run the affected suites after every change and the full suite before saying anything is done. Let a failing test localize a bug instead of guessing at fixes.
